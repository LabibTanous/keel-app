// Supabase replaced by Convex. Stub kept to avoid breaking residual imports.
export function getSupabase(): never {
  throw new Error("Supabase is not configured — this app uses Convex for persistence.");
}
